version: '3.7'

services:
  flask:
    build: ./service
    ports:
      - '5000:5000'
    networks:
      - ata-network

  nginx:
    build: ./frontend
    ports:
      - '80:80'
      - '443:443'
    networks:
      - ata-network

networks:
  ata-network:
    driver: bridge

